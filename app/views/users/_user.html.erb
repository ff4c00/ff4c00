<li>
	<%= gravatar_for(user: user)%>
  <%= link_to user.name, user_path(user)%>
	<% if current_user.admin? && !(current_user?(user)) %>
		|<%= link_to '删除用户', user, method: :delete, data: {confirm: '确定删除该用户?'} %>
	<% end %>
</li>

