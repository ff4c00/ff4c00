<% provide(:title, (title = '用户列表'))%>
<h1><%= title %></h1>

<ul class='users'>
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for(user: user)%>
      <%= link_to user.name, user_path(user)%>
    </li>
  <% end %>
</ul>
2
<%= paginate @users %>