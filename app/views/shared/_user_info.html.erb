<%= link_to gravatar_for(user: current_user, size:50), current_user %>
<h2><%= current_user.name%></h2>
<span><%= link_to '详情', current_user%></span>
<span>微博(<%= current_user.microposts.count %>)</span>
